<% provide(:title, @game.name) %>

<p>
	<% if game_pending?(@game) %>
  <strong>Game <%= @game.name %> is Pending. Would you like to
  	<%= link_to 'join?', '#' %></strong> 
  	<% else %>

  		<% if game_done?(@game) %>
	  		<strong>Game <%= @game.name %> is Over.</strong> 
	  	<% else %>
	  		<strong> Game <%= @game.name %> is on-going.</strong>
  		<% end %>
  	<% end %>

  	
</p>


<%= link_to 'Leave', games_path(:leave => 1) %> |
<%= link_to 'Back', games_path %>